{
  "name": "videocallapp",
  "private": true,
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build && cp -r ./dist ../server/dist",
    "start": "cd server && node src/index.js"
  }
}
